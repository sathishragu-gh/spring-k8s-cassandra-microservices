spring.application.name: spring-boot-service

server:
  port: 8083

management:
  server:
    port: 8084

# Local Cassandra defaults — override with env vars DB_USERNAME / DB_PASSWORD if needed
cassandra.contact-points: localhost
cassandra.keyspace: betterbotz
cassandra.local-datacenter: datacenter1
cassandra.port: 9042

---
spring.profiles: docker
cassandra.contact-points: host.docker.internal
cassandra.keyspace: betterbotz
cassandra.local-datacenter: dc1
cassandra.port: 9042